[package]
name = "creusot-discount-passing"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
creusot-std = "0.12.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(creusot)"] }
