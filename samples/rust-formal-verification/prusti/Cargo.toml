[package]
name = "prusti-discount-verification"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
prusti-contracts = "0.1.6"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(prusti)"] }
