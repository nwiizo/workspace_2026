[package]
name = "requirements-evidence-rust"
version = "0.1.0"
edition = "2024"
publish = false

[dev-dependencies]
proptest = "1.10.0"

[[bench]]
name = "transfer"
harness = false

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(kani)"] }
