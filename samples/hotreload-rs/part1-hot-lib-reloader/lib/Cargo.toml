[package]
name = "hot-lib"
version = "0.1.0"
edition = "2024"

[lib]
# dylib when hot-reloading, rlib for normal builds
crate-type = ["rlib", "dylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
