services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: subnetmap
      POSTGRES_PASSWORD: subnetmap
      POSTGRES_DB: subnetmap
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
