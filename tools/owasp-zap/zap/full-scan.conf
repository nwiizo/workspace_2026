# zap-full-scan.py rule configuration.
# Change WARN to IGNORE to ignore rule or FAIL to fail if rule matches.
#
# The DOM XSS rule starts a browser. It is intentionally skipped in this
# local lab because the regular reflected XSS active rule covers the vulnerable
# Rust endpoint and the browser rule can be unstable in containerized arm64
# environments.
40026	IGNORE	(Cross Site Scripting - DOM Based)
